# Commits that changed formatting only. `git blame` skips these so it keeps
# pointing at whoever actually wrote a line.
#
# Configure git to use this automatically:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub reads it without any configuration.

# Apply Prettier
d560f46366534d08a583bf70be5a3bf3c5f7f249
