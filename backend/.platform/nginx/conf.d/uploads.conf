# Elastic Beanstalk's default nginx reverse proxy caps request bodies at
# 1MB, which is fine for JSON API calls but too small for newsletter
# uploads (PDF/.eml files routinely exceed that). Raised to match the
# multer fileSize limit in routes/newsletters.js.
client_max_body_size 20M;
